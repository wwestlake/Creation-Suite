# Shared Node System

This module is reserved for node graph infrastructure that is genuinely
cross-application.

Creation Engine already contains the strongest current node foundation,
but only the app-agnostic parts should move here.
