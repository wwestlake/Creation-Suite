# Shared Asset System

This module will become the suite-wide home for:

- shared asset identity types
- suite discovery/bootstrap metadata
- zip-backed virtual filesystem access
- project archive conventions
- temp materialization rules for integrations that require real files

The current first-step implementation work started in Creation Station on
July 28, 2026 and should be migrated here next.
